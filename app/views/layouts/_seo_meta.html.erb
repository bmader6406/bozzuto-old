<% if item.meta_title.present? %>
  <% content_for(:meta_title) do %><%= item.meta_title %><% end %>
<% end %>

<% if item.meta_description.present? %>
  <% content_for(:meta_description) do %><%= item.meta_description %><% end %>
<% end %>

<% if item.meta_keywords.present? %>
  <% content_for(:meta_keywords) do %><%= item.meta_keywords %><% end %>
<% end %>
