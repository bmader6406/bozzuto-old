<% breadcrumb_item(@page.title) %>

<% breadcrumb_title(@page.title) %>


<%= @page.body.try(:html_safe) %>

<% all_home_communities.each do |community| %>
  <%= link_to community.title, green_home_path(community) %><br>
<% end %>
