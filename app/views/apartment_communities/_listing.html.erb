<li class="property-listing apartment-listing">
  <p class="photos">
    <% if community.listing_image? %>
      <%= image_tag community.listing_image.url %>
    <% end %>
  </p>

  <div class="info">
    <div class="overview">
      <h2><%= community.title %></h2>
      <p class="address-info">
        <%= community.address %><br />
        <%= community.phone_number %>
      </p>

      <ul>
        <li><strong>1 Bedrooms</strong> from $XXXX</li>
        <li><strong>2 Bedrooms</strong> from $XXXX</li>
        <li><strong>3 Bedrooms</strong> from $XXXX</li>
      </ul>

      <% if community.overview_bullet_1.present? %>
        <p class="description"><%= community.overview_bullet_1 %></p>
      <% end %>
    </div>

    <div class="learn-more">
      <h2>
        <%= link_to 'Learn More', community %>
      </h2>

      <% if community.availability_url.present? %>
        <p><%= link_to 'Availability', community.availability_url %></p>
      <% end %>

      <%= property_icons(community) %>
    </div>
  </div>
</li>
