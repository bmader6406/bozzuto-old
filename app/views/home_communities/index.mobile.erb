<% render_meta(@page) if @page.present? %>


<div class="section page-title">
  <div class="content">
    <h2>Buy a Home</h2>

    <%= link_to 'Home', root_path, :class => 'go-back' %>
  </div>
</div>

<div class="section property-list">
  <div class="content">
    <ul>
      <%= render :partial => 'home_communities/listing', :collection => @communities, :as => :community %>
    </ul>
  </div>
</div>
