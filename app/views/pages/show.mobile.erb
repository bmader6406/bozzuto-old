<% render_meta(@page) %>

<%# @page.ancestors.each do |page| %>
  <%# breadcrumb_item(link_to page.title, page_path(@section, page)) %>
<%# end %>
<%# breadcrumb_item(@page.title) %>

<%# breadcrumb_title(@page.title) %>


<div class="section page-title">
  <div class="content">
    <h2><%= @page.title %></h2>

    <% if @page.ancestors.any? %>
      <% back_page = @page.ancestors.last %>
      <%= link_to back_page.title, page_path(@section, back_page), :class => 'go-back' %>
    <% else %>
      <%= link_to 'Home', root_url, :class => 'go-back' %>
    <% end %>
  </div>
</div>

<div class="section services">
  <div class="content">
    <%= @page.body.try(:html_safe) %>

    <% if @page.section.cached_slug == 'services' && @page.first? %>
      <ul>
        <li>
          <%= link_to 'Management', '/services/management', :class => 'btn' %>
        </li>
        <li>
          <%= link_to 'Homebuilding', '/services/homebuilding', :class => 'btn' %>
        </li>
        <li>
          <%= link_to 'Construction', '/services/construction', :class => 'btn' %>
        </li>
        <li>
          <%= link_to 'Acquisitions', '/services/acquisitions', :class => 'btn' %>
        </li>
        <li>
          <%= link_to 'Land', '/services/land', :class => 'btn' %>
        </li>
      </ul>
    <% end %>
  </div>
</div>
