<% render_meta(@page) %>

<% @page.ancestors.each do |page| %>
  <% breadcrumb_item(link_to page.title, page_path(@section, page)) %>
<% end %>
<% breadcrumb_item(@page.title) %>

<% breadcrumb_title(@page.title) %>

<%= @page.body.try(:html_safe) %>


<% if params[:controller] == 'pages' && params[:section] == 'apartments' && params[:page].empty? %>
<% end %>

<% if params[:section] == 'apartments' %>

  <% if @page.cached_slug == 'overview' %>
    <!-- Apartments Landing Page -->

    <iframe src="http://img-cdn.mediaplex.com/0/16798/universal.html?page_name=bozzuto_apts_lp&Bozzuto_Apts_LP=1&mpuid=" HEIGHT="1" WIDTH="1" FRAMEBORDER="0"></iframe>

    <% content_for :javascript do %>
			<script type="text/javascript" src="http://js.washingtonpost.com/wp-adv/units/wp-plus/102813/Retargeting3AptHome/tag.js"></script>
    <% end %>

  <% end %>

<% end %>
