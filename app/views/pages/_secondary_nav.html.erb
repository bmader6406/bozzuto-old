<% if @section.present? %>
  <ul id="secondary-nav">
    <%= pages_tree(@section.pages.published.for_sidebar_nav) %>

    <%= news_and_press_secondary_nav(@section, @news_section) %>

    <% if @section.about? %>
      <li class="<%= current_if :controller => 'contact_submissions' %>">
        <%= link_to 'Contact', contact_path %>
      </li>
    <% end %>
  </ul>
<% else %>
  &nbsp;
<% end %>
